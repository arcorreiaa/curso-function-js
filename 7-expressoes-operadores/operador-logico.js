// operador logico

// 2 valores booleanos, quando verificados
// resultara em verdadeiro ou falso

let pao = true;
let queijo = true;

// and &&
console.log(pao && queijo);

// or ||
console.log(pao || queijo);

// not !
console.log(!pao);
